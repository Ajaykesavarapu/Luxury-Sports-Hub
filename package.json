{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build --prefix client && rm -rf dist && cp -r client/dist dist || rmdir /s /q dist & xcopy /E /I /Y client\\dist dist",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run dev --prefix client"
  },
  "private": true,
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "@vercel/node": "^5.0.2"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.8",
    "typescript": "~5.9.2"
  }
}
