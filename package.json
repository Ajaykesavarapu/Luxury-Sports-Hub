{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @workspace/client run build",
    "dev:client": "pnpm --filter @workspace/client run dev",
    "dev": "pnpm --filter @workspace/client run dev"
  },
  "private": true,
  "devDependencies": {
    "@vercel/node": "^5.1.8",
    "typescript": "~5.9.2"
  }
}
