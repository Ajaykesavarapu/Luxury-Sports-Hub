{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cd client && npm install && npx vite build",
    "dev:client": "cd client && npx vite --host 0.0.0.0",
    "dev": "cd client && npx vite --host 0.0.0.0"
  },
  "private": true,
  "devDependencies": {
    "@vercel/node": "^5.1.8",
    "typescript": "~5.9.2"
  }
}
