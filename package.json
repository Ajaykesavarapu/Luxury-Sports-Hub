{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npm run build --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run dev --prefix client"
  },
  "private": true,
  "devDependencies": {
    "@vercel/node": "^5.1.8",
    "typescript": "~5.9.2"
  }
}
