{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./client\" --filter \"./server\" --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:client": "pnpm --filter @workspace/client dev",
    "dev:server": "pnpm --filter @workspace/server dev",
    "dev": "node scripts/dev.mjs"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "package.json": "^2.0.1"
  }
}
